apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis
  namespace: workloads
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: redis
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 400m
              memory: 512Mi
